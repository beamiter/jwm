/target
# The default release bundle is built with --locked, so its application lock
# file is committed alongside the bar just like the root JWM lockfile.
**/*.rs.bk
*.pdb
.idea/
.vscode/
*.swp
*.swo
node_modules/
