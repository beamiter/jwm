# MediaPipe 0.10.35 removed the legacy mp.solutions API used by the tracker.
mediapipe==0.10.21
numpy==1.26.4
opencv-contrib-python==4.11.0.86
mss==10.2.0

# Pin the last NumPy 1.x-compatible JAX line to prevent pip from backtracking
# through many large jaxlib wheels while resolving MediaPipe's open dependency.
jax==0.4.35
jaxlib==0.4.35
