# Keep the repository build environment host-independent. The portal discovers
# PipeWire and libclang through their normal pkg-config/bindgen mechanisms.
# Developers with a private PipeWire prefix can set JWM_PIPEWIRE_PREFIX; the
# portal installer derives PKG_CONFIG_PATH and an rpath from that value.
